**Status:** draft (pending manual post)

**HN:** https://news.ycombinator.com/item?id=47986839

**Story:** "Claude Code, Copilot and Codex all got hacked. Every attacker went for the credentials" (venturebeat.com), submitted by `mooreds`, 1 point, 0 comments, 16h old at draft time. Article URL: https://venturebeat.com/security/six-exploits-broke-ai-coding-agents-iam-never-saw-them

**OP:** Submission of a VentureBeat piece by Louis Columbus walking through six 2025-2026 exploits against Codex, Claude Code, Copilot, and Vertex AI agents. The unifying thesis: every exploit went after the agent's runtime credential, not the model. Specifics: (1) BeyondTrust's GitHub OAuth token exfil via a branch-name command-injection in Codex setup, hidden behind 94 U+3000 chars; (2) CVE-2026-25723 piped sed/echo escape of Claude Code's project sandbox; (3) CVE-2026-33068 .claude/settings.json `permissions.defaultMode = bypassPermissions` loaded before the workspace trust dialog; (4) Adversa's finding that Claude Code silently dropped deny-rule enforcement after the 50th subcommand in a chain; (5) CVE-2025-53773 hidden PR-description instructions flipping Copilot's `auto-approve` in `.vscode/settings.json` for unrestricted shell; (6) Orca's Copilot-in-Codespaces RCE via a hidden GitHub-issue instruction → symlink to `/workspaces/.codespaces/shared/user-secrets-envs.json` exfiltrating GITHUB_TOKEN. Article quotes Merritt Baer (Enkrypt), Carter Rees (Reputation), Mike Riemer (Ivanti), Elia Zaitsev (CrowdStrike).

**My reply:**

```
(disclosure: I work on FailProof AI: https://github.com/exospherehost/failproofai)

The Adversa finding is the one that should worry operators most. Claude Code silently dropped its own deny-rule enforcement after the 50th subcommand in a chain. The work still ran, the gate just stopped firing. That is the structural lesson the article underlines: any policy that lives in the same process as the agent is one performance trade-off away from being silently skipped, and you find out from a researcher rather than from a log line.

The cheap mitigation is to keep the gate in a different process from the agent loop. Claude Code's PreToolUse hook is one place: a hook script that re-checks each Bash invocation against your deny patterns runs in a fresh subprocess every call, so the agent's internal truncation cannot disable it. Same pattern catches the symlink-to-secrets-file read Orca demonstrated against Copilot in Codespaces.
```

**Insight for the FailProof team:** This article is the cleanest external articulation of the "policy-must-live-outside-the-agent" thesis we have been writing about (see PR #17 on the Mendral.com agent-harness piece). It provides six concrete exploit primitives operators can cite when explaining why an out-of-process hook layer is not paranoia. The Adversa 50-subcommand bypass is *especially* useful copy: it is a vendor-acknowledged correctness regression in the agent's own enforcement plane, fixed only after disclosure. That is the strongest "don't trust the in-process gate" example in public to date. Worth a follow-up blog post that lines up each of the six exploit classes with the FailProof policy or hook-pattern that would have caught it (without doing the comma-list in any HN comment). Also: `mooreds` is a serial submitter of security articles; following his /threads in the future may surface more on-topic submissions before they accumulate noise.

**Notes / findings:**
- The HN thread is dead at draft time (1 point, 0 comments). Visibility for any comment is near zero. Posting still has signal because (a) a first comment that adds the operator's-eye view often draws clickthroughs from /newest browsers later, and (b) when the next person searches HN for "Claude Code deny rule" or "Adversa 50 subcommand" the comment is what they will land on. Risk: a first comment on a dead thread can read as keyword-hunting if the body is shallow. Mitigation here is that the body engages with a specific finding by name (Adversa, 50-subcommand) and offers a concrete mitigation (PreToolUse hook subprocess), not a product pitch.
- ASCII punctuation throughout. No em-dashes, no fancy ellipses, no curly quotes. Three ASCII dots and straight quotes only.
- One link in the body (the disclosure repo URL). No second link at the bottom.
- One mitigation pattern named (PreToolUse hook), not a comma-list of policies. No install command. No version numbers. No three-scope or `~/.failproofai/` callouts.
- The article is not paywalled; full content was readable via browser.
- Cross-thread guard: this body is materially different from PR #17's harness-outside-sandbox draft (which addresses the same general thesis but on a different OP and with a different anchor). The shared word here is "process" / "subprocess" — that is the actual mitigation, not borrowed phrasing.
