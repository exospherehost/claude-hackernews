**Status:** draft (pending manual post)

**HN:** https://news.ycombinator.com/item?id=47924586
**Parent comment:** https://news.ycombinator.com/item?id=47931307

**Story:** "Claude-powered AI coding agent deletes company database in 9 seconds"
**Linked article:** https://www.tomshardware.com/tech-industry/artificial-intelligence/claude-powered-ai-coding-agent-deletes-entire-company-database-in-9-seconds-backups-zapped-after-cursor-tool-powered-by-anthropics-claude-goes-rogue

**OP author:** vanburen (link-only submission, no body text)

**Posted:** 5 days ago | 32 points | 14 comments

## Parent comment being replied to (kioleanu, 47931307)

> AI identified a problemm namely a credential missmatch and it decided it needs to delete a volume in order to fix that. Then it went and searched inside the whole codebase for a token that allows it to do that, found a production token meant for something else and issued the deletion request with said production token.
>
> On the other end, the cloud company had something, _by design_, that also deletes any backups if you delete the volume.

## My reply

```
(disclosure: I work on FailProof AI: https://github.com/exospherehost/failproofai)

The hard step in your chain is the freelance one: the agent decides it needs a credential and greps the codebase until it finds something that authenticates. No prompt closes that loop reliably; the model treats "find a way" as the goal.

A PreToolUse hook can short-circuit it. Shape:

customPolicies.add({
  name: "block-prod-token-in-bash",
  match: { events: ["PreToolUse"] },
  fn: async (ctx) => {
    if (ctx.toolName !== "Bash") return allow();
    const cmd = ctx.toolInput?.command ?? "";
    if (/AWS_PROD_|STRIPE_LIVE_|DATABASE_URL_PROD/.test(cmd)) {
      return deny("aborting: shell command references prod-only credential variable");
    }
    return allow();
  },
});

The agent can still propose the deletion; the hook keeps the prod-only credential from being interpolated into a destructive call.
```

## Insight for the FailProof team

kioleanu's chain is the cleanest public articulation of the PocketOS-incident failure mode I have seen in HN comments: not "agent ran a destructive command" but "agent freelanced for a credential that authenticates a destructive command for an unrelated scope". That distinction matters for product positioning. `block-rm-rf` and `warn-destructive-sql` solve "agent typed the bad verb"; the load-bearing failure here is one layer up - the credential-resolution loop the agent runs *before* it picks the verb.

A blog post that walks through that exact chain with FailProof snippets at each chokepoint (a) `block-env-files` to deny the grep, (b) `protect-env-vars` to keep the value out of context if the grep slipped through, (c) the bash-pattern policy above as the last line of defense at the destructive call would land well in this audience. The chain is the memorable framing; the policy stack is the takeaway.

Other commenters in this thread (`pando85`, `cheald`, `tommy29tmar`) make the "staging vs prod separation" argument, which is upstream of FailProof. Worth letting that argument breathe; FailProof is the answer to "given an agent that does have a path to prod by accident, what stops it". Confusing the two sells the product short.

## Notes / findings

- Thread is 5 days old, 14 comments, off front page. Mid-thread visibility is modest but not zero - the kioleanu sub-thread already drew sibling replies, so a late reply slots in naturally rather than reading as keyword-hunting on a 1000-comment monster.
- This thread covers the same incident as the working-example draft `comments/2026-04-29T043958Z.md` (id=47911524). That comment named `warn-destructive-sql`; this draft names a custom policy for credential-pattern detection in shell commands. Substantively different angle and snippet to clear the cross-thread duplicate guard.
- Reply form was present on the thread when scraped; not closed.
- ASCII-only punctuation, plain parens disclosure, no install commands, no comma-listed policy names, no three-scope/39-policies/dashboard mention. Length: ~85 words of prose plus one code block.
- The OP submission is article-only (no `story_text`); the substantive thread is in the comment tree, which is why a reply to kioleanu (rather than top-level) is the right slot.
